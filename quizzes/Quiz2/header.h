#ifndef __HEADER_H__CR4
#define __HEADER_H__CR4

#include <stdio.h>/* for printf*/

/*function that print T F and TF for all numbers up to num (included) if the number is divisible by 3, 5 or both accordingly or the number*/
void TF(int num);

/*turn Capital into lowercase*/
char ToLower(char c);

/*Reverse a string in place*/
void StrReverser(char *str);

#endif
